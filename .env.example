# EIDOLON does not require any API keys. This file documents optional
# local configuration only.

# Base URL of your local Ollama daemon (default shown below).
OLLAMA_HOST=http://localhost:11434

# Seconds to wait for a single Ollama generation before treating it as a
# timeout.
OLLAMA_TIMEOUT_SECONDS=60
